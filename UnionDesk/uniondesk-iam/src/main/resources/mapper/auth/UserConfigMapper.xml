<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.auth.mapper.UserConfigMapper">

    <resultMap id="UserConfigPo" type="com.uniondesk.auth.entity.UserConfigPo">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="configKey" column="config_key"/>
        <result property="configValue" column="config_value"/>
        <result property="valueType" column="value_type"/>
        <result property="description" column="description"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectByUserIdAndKey" resultMap="UserConfigPo">
        SELECT id, user_id, config_key, config_value, value_type, description, updated_at
        FROM user_config
        WHERE user_id = #{userId}
          AND config_key = #{configKey}
        LIMIT 1
    </select>

    <insert id="upsert">
        INSERT INTO user_config (
            user_id, config_key, config_value, value_type, description
        )
        VALUES (#{userId}, #{configKey}, #{configValue}, #{valueType}, #{description})
        ON DUPLICATE KEY UPDATE
            config_value = VALUES(config_value),
            value_type = VALUES(value_type),
            description = VALUES(description),
            updated_at = CURRENT_TIMESTAMP(3)
    </insert>

</mapper>
