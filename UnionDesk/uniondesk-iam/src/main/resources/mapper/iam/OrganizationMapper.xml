<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.iam.mapper.OrganizationMapper">

    <resultMap id="OrganizationPo" type="com.uniondesk.iam.entity.OrganizationPo">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="parentId" column="parent_id"/>
        <result property="parentName" column="parent_name"/>
        <result property="leaderUserId" column="leader_user_id"/>
        <result property="leaderName" column="leader_name"/>
        <result property="orderNo" column="order_no"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <sql id="organizationSelect">
        SELECT
            o.id,
            o.code,
            o.name,
            o.parent_id,
            parent.name AS parent_name,
            o.leader_user_id,
            leader.username AS leader_name,
            o.order_no,
            o.status,
            o.remark,
            o.created_at
        FROM platform_organization o
        LEFT JOIN platform_organization parent ON parent.id = o.parent_id
        LEFT JOIN user_account leader ON leader.id = o.leader_user_id
    </sql>

    <select id="selectAll" resultMap="OrganizationPo">
        <include refid="organizationSelect"/>
        ORDER BY COALESCE(o.parent_id, 0), o.order_no, o.id
    </select>

    <select id="selectById" resultMap="OrganizationPo">
        <include refid="organizationSelect"/>
        WHERE o.id = #{id}
        LIMIT 1
    </select>

    <select id="selectByCode" resultMap="OrganizationPo">
        <include refid="organizationSelect"/>
        WHERE o.code = #{code}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.uniondesk.iam.entity.OrganizationPo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO platform_organization (
            code, name, parent_id, leader_user_id, order_no, status, remark, created_at, updated_at
        )
        VALUES (#{code}, #{name}, #{parentId}, #{leaderUserId}, #{orderNo}, #{status}, #{remark},
                CURRENT_TIMESTAMP(3), CURRENT_TIMESTAMP(3))
    </insert>

    <update id="update" parameterType="com.uniondesk.iam.entity.OrganizationPo">
        UPDATE platform_organization
        SET code = #{code},
            name = #{name},
            parent_id = #{parentId},
            leader_user_id = #{leaderUserId},
            order_no = #{orderNo},
            status = #{status},
            remark = #{remark},
            updated_at = CURRENT_TIMESTAMP(3)
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM platform_organization WHERE id = #{id}
    </delete>

    <select id="countById" resultType="int">
        SELECT COUNT(*) FROM platform_organization WHERE id = #{id}
    </select>

    <select id="countByParentId" resultType="int">
        SELECT COUNT(*) FROM platform_organization WHERE parent_id = #{parentId}
    </select>

    <select id="selectParentId" resultType="java.lang.Long">
        SELECT parent_id FROM platform_organization WHERE id = #{id}
    </select>

    <select id="selectChildIds" resultType="java.lang.Long">
        SELECT id FROM platform_organization WHERE parent_id = #{parentId}
    </select>

    <select id="selectUserOrganizationIds" resultType="java.lang.Long">
        SELECT organization_id FROM user_organization WHERE user_id = #{userId} ORDER BY organization_id
    </select>

    <delete id="deleteUserOrganizations">
        DELETE FROM user_organization WHERE user_id = #{userId}
    </delete>

    <insert id="insertUserOrganization">
        INSERT INTO user_organization (user_id, organization_id) VALUES (#{userId}, #{organizationId})
    </insert>

    <select id="countUserAccountById" resultType="int">
        SELECT COUNT(*) FROM user_account WHERE id = #{id}
    </select>

</mapper>
