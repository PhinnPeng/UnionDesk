<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.iam.mapper.UserAccountMapper">

    <resultMap id="UserSummaryPo" type="com.uniondesk.iam.entity.UserSummaryPo">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="mobile" column="mobile"/>
        <result property="email" column="email"/>
    </resultMap>

    <select id="selectSummaryById" resultMap="UserSummaryPo">
        SELECT id, username, phone AS mobile, email
        FROM staff_account
        WHERE id = #{id}
        LIMIT 1
    </select>

</mapper>
