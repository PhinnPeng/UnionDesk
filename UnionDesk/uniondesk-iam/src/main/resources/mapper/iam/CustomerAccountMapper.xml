<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.iam.mapper.CustomerAccountMapper">

    <insert id="insertRow" parameterType="com.uniondesk.iam.entity.CustomerAccountPo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO customer_account (
            subject_id, username, nickname, avatar_url, phone, email, password_hash,
            must_change_password, status, source, auth_version, password_changed_at, created_at, updated_at
        )
        VALUES (#{subjectId}, #{username}, #{nickname}, NULL, #{phone}, #{email}, #{passwordHash},
                #{mustChangePassword}, 'active', 'local', 1, CURRENT_TIMESTAMP(3), CURRENT_TIMESTAMP(3), CURRENT_TIMESTAMP(3))
    </insert>

    <update id="updateProfile">
        UPDATE customer_account
        SET nickname = #{nickname},
            phone = #{phone},
            email = #{email},
            real_name = #{realName},
            <if test="idCardNo != null">
                id_card_no = #{idCardNo},
            </if>
            updated_at = CURRENT_TIMESTAMP(3)
        WHERE id = #{id}
    </update>

</mapper>
