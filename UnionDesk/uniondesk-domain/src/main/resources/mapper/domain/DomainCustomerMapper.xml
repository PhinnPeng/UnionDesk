<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.domain.mapper.DomainCustomerMapper">

    <resultMap id="DomainCustomerPo" type="com.uniondesk.domain.entity.DomainCustomerPo">
        <id property="id" column="id"/>
        <result property="businessDomainId" column="business_domain_id"/>
        <result property="customerAccountId" column="customer_account_id"/>
        <result property="subjectId" column="subject_id"/>
        <result property="username" column="username"/>
        <result property="nickname" column="nickname"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="status" column="status"/>
        <result property="source" column="source"/>
        <result property="activatedAt" column="activated_at"/>
        <result property="disabledAt" column="disabled_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="customerListWhere">
        <where>
            dc.business_domain_id = #{domainId}
            AND dc.deleted_at IS NULL
            <if test="status != null and status != ''">
                AND dc.status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (ca.username LIKE #{keyword}
                     OR ca.nickname LIKE #{keyword}
                     OR ca.phone LIKE #{keyword}
                     OR ca.email LIKE #{keyword})
            </if>
        </where>
    </sql>

    <select id="listCustomers" resultMap="DomainCustomerPo">
        SELECT
            dc.id,
            dc.business_domain_id,
            dc.customer_account_id,
            ca.subject_id,
            ca.username,
            ca.nickname,
            ca.phone,
            ca.email,
            dc.status,
            dc.source,
            dc.activated_at,
            dc.disabled_at,
            dc.created_at,
            dc.updated_at
        FROM domain_customer dc
        JOIN customer_account ca ON ca.id = dc.customer_account_id
        JOIN identity_subject s ON s.id = ca.subject_id
        <include refid="customerListWhere"/>
        ORDER BY dc.id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countCustomers" resultType="long">
        SELECT COUNT(*)
        FROM domain_customer dc
        JOIN customer_account ca ON ca.id = dc.customer_account_id
        JOIN identity_subject s ON s.id = ca.subject_id
        <include refid="customerListWhere"/>
    </select>

    <select id="selectById" resultMap="DomainCustomerPo">
        SELECT
            dc.id,
            dc.business_domain_id,
            dc.customer_account_id,
            ca.subject_id,
            ca.username,
            ca.nickname,
            ca.phone,
            ca.email,
            dc.status,
            dc.source,
            dc.activated_at,
            dc.disabled_at,
            dc.created_at,
            dc.updated_at
        FROM domain_customer dc
        JOIN customer_account ca ON ca.id = dc.customer_account_id
        JOIN identity_subject s ON s.id = ca.subject_id
        WHERE dc.id = #{id}
          AND dc.business_domain_id = #{domainId}
          AND dc.deleted_at IS NULL
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO domain_customer (
            customer_account_id,
            business_domain_id,
            status,
            source,
            activated_at,
            disabled_at,
            deleted_at,
            created_at,
            updated_at
        )
        VALUES (
            #{customerAccountId}, #{businessDomainId}, 'active', #{source},
            #{activatedAt}, NULL, NULL,
            CURRENT_TIMESTAMP(3), CURRENT_TIMESTAMP(3)
        )
    </insert>

    <select id="countByDomainAndCustomer" resultType="int">
        SELECT COUNT(*)
        FROM domain_customer
        WHERE business_domain_id = #{domainId}
          AND customer_account_id = #{customerAccountId}
          AND deleted_at IS NULL
    </select>

    <update id="updateStatus">
        UPDATE domain_customer
        SET status = #{status},
            activated_at = #{activatedAt},
            disabled_at = #{disabledAt},
            updated_at = CURRENT_TIMESTAMP(3)
        WHERE id = #{id}
          AND business_domain_id = #{domainId}
          AND deleted_at IS NULL
    </update>

</mapper>
