<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.ticket.mapper.TicketTypeFlowTransitionMapper">

    <resultMap id="BaseResultMap" type="com.uniondesk.ticket.entity.TicketTypeFlowTransitionPo">
        <id column="id" property="id"/>
        <result column="domain_id" property="domainId"/>
        <result column="ticket_type_id" property="ticketTypeId"/>
        <result column="from_state_code" property="fromStateCode"/>
        <result column="to_state_code" property="toStateCode"/>
        <result column="step_name" property="stepName"/>
        <result column="permission_mode" property="permissionMode"/>
        <result column="member_ids" property="memberIdsJson"/>
        <result column="role_ids" property="roleIdsJson"/>
        <result column="required_slot_ids" property="requiredSlotIdsJson"/>
        <result column="attribute_updates" property="attributeUpdatesJson"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="findByDomainIdAndTypeId" resultMap="BaseResultMap">
        SELECT * FROM ticket_type_flow_transition
        WHERE domain_id = #{domainId}
          AND ticket_type_id = #{ticketTypeId}
        ORDER BY sort_order ASC, id ASC
    </select>

    <insert id="batchInsert">
        INSERT INTO ticket_type_flow_transition (
            domain_id, ticket_type_id, from_state_code, to_state_code, step_name,
            permission_mode, member_ids, role_ids, required_slot_ids, attribute_updates,
            sort_order
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.domainId}, #{item.ticketTypeId}, #{item.fromStateCode}, #{item.toStateCode}, #{item.stepName},
                #{item.permissionMode}, #{item.memberIdsJson}, #{item.roleIdsJson}, #{item.requiredSlotIdsJson}, #{item.attributeUpdatesJson},
                #{item.sortOrder}
            )
        </foreach>
    </insert>

    <delete id="deleteByDomainIdAndTypeId">
        DELETE FROM ticket_type_flow_transition
        WHERE domain_id = #{domainId}
          AND ticket_type_id = #{ticketTypeId}
    </delete>

    <delete id="deleteByDomainIdAndTypeIdAndStateCode">
        DELETE FROM ticket_type_flow_transition
        WHERE domain_id = #{domainId}
          AND ticket_type_id = #{ticketTypeId}
          AND (from_state_code = #{stateCode} OR to_state_code = #{stateCode})
    </delete>

</mapper>
