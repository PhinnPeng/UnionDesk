<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.ticket.mapper.TicketStatusMapper">

    <resultMap id="TicketStatusPo" type="com.uniondesk.ticket.entity.TicketStatusPo">
        <id property="id" column="id"/>
        <result property="scope" column="scope"/>
        <result property="businessDomainId" column="business_domain_id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="category" column="category"/>
        <result property="stateType" column="state_type"/>
        <result property="configJson" column="config_json"/>
        <result property="status" column="status"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="system" column="is_system"/>
        <result property="sourceStatusId" column="source_status_id"/>
        <result property="createdBy" column="created_by"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="TicketStatusColumns">
        id, scope, business_domain_id, code, name, description, category, state_type,
        config_json, status, sort_order, is_system, source_status_id, created_by, updated_by,
        created_at, updated_at
    </sql>

    <sql id="platformScopeWhere">
        scope = 'platform'
        <if test="keywordLike != null and keywordLike != ''">
            AND (name LIKE #{keywordLike} OR description LIKE #{keywordLike} OR code LIKE #{keywordLike})
        </if>
    </sql>

    <select id="findByPlatform" resultMap="TicketStatusPo">
        SELECT <include refid="TicketStatusColumns"/>
        FROM ticket_status
        WHERE <include refid="platformScopeWhere"/>
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="countByPlatform" resultType="long">
        SELECT COUNT(1)
        FROM ticket_status
        WHERE <include refid="platformScopeWhere"/>
    </select>

    <select id="findPageByPlatform" resultMap="TicketStatusPo">
        SELECT <include refid="TicketStatusColumns"/>
        FROM ticket_status
        WHERE <include refid="platformScopeWhere"/>
        ORDER BY sort_order ASC, id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findById" resultMap="TicketStatusPo">
        SELECT <include refid="TicketStatusColumns"/>
        FROM ticket_status
        WHERE id = #{id}
    </select>

    <select id="findPlatformByCode" resultMap="TicketStatusPo">
        SELECT <include refid="TicketStatusColumns"/>
        FROM ticket_status
        WHERE scope = 'platform' AND code = #{code}
        LIMIT 1
    </select>

    <select id="findPlatformByName" resultMap="TicketStatusPo">
        SELECT <include refid="TicketStatusColumns"/>
        FROM ticket_status
        WHERE scope = 'platform' AND name = #{name}
        LIMIT 1
    </select>

    <select id="findMaxSortOrderPlatform" resultType="java.lang.Integer">
        SELECT MAX(sort_order)
        FROM ticket_status
        WHERE scope = 'platform'
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ticket_status (
            scope, business_domain_id, code, name, description, category, state_type,
            config_json, status, sort_order, is_system, source_status_id, created_by, updated_by
        ) VALUES (
            #{scope}, #{businessDomainId}, #{code}, #{name}, #{description}, #{category}, #{stateType},
            #{configJson}, #{status}, #{sortOrder}, #{system}, #{sourceStatusId}, #{createdBy}, #{updatedBy}
        )
    </insert>

    <update id="update">
        UPDATE ticket_status
        SET name = #{name},
            description = #{description},
            category = #{category},
            state_type = #{stateType},
            config_json = #{configJson},
            status = #{status},
            updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>

    <delete id="deleteByIdPlatform">
        DELETE FROM ticket_status
        WHERE id = #{id}
          AND scope = 'platform'
          AND is_system = 0
    </delete>

</mapper>
