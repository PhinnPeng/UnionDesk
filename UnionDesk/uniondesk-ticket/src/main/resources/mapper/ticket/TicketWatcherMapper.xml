<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.ticket.mapper.TicketWatcherMapper">

    <select id="findStaffIdsByTicketId" resultType="long">
        SELECT staff_account_id
        FROM ticket_watcher
        WHERE ticket_id = #{ticketId}
        ORDER BY id ASC
    </select>

    <delete id="deleteByTicketId">
        DELETE FROM ticket_watcher WHERE ticket_id = #{ticketId}
    </delete>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ticket_watcher (ticket_id, staff_account_id, created_at)
        VALUES (#{ticketId}, #{staffAccountId}, CURRENT_TIMESTAMP(3))
    </insert>

</mapper>
