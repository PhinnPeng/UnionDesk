<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.ticket.mapper.TicketAttributeMapper">

    <resultMap id="TicketAttributePo" type="com.uniondesk.ticket.entity.TicketAttributePo">
        <id property="id" column="id"/>
        <result property="scope" column="scope"/>
        <result property="businessDomainId" column="business_domain_id"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="fieldType" column="field_type"/>
        <result property="typeConfig" column="type_config"/>
        <result property="status" column="status"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="system" column="is_system"/>
        <result property="systemKey" column="system_key"/>
        <result property="sourceAttributeId" column="source_attribute_id"/>
        <result property="createdBy" column="created_by"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="TicketAttributeColumns">
        id, scope, business_domain_id, name, description, field_type, type_config,
        status, sort_order, is_system, system_key, source_attribute_id, created_by, updated_by,
        created_at, updated_at
    </sql>

    <sql id="platformScopeWhere">
        scope = 'platform'
        <if test="keywordLike != null and keywordLike != ''">
            AND (name LIKE #{keywordLike} OR description LIKE #{keywordLike})
        </if>
    </sql>

    <sql id="domainScopeWhere">
        scope = 'domain'
        AND business_domain_id = #{domainId}
        <if test="keywordLike != null and keywordLike != ''">
            AND (name LIKE #{keywordLike} OR description LIKE #{keywordLike})
        </if>
    </sql>

    <select id="findByPlatform" resultMap="TicketAttributePo">
        SELECT <include refid="TicketAttributeColumns"/>
        FROM ticket_attribute
        WHERE <include refid="platformScopeWhere"/>
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="findByDomain" resultMap="TicketAttributePo">
        SELECT <include refid="TicketAttributeColumns"/>
        FROM ticket_attribute
        WHERE <include refid="domainScopeWhere"/>
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="countByPlatform" resultType="long">
        SELECT COUNT(1)
        FROM ticket_attribute
        WHERE <include refid="platformScopeWhere"/>
    </select>

    <select id="countByDomain" resultType="long">
        SELECT COUNT(1)
        FROM ticket_attribute
        WHERE <include refid="domainScopeWhere"/>
    </select>

    <select id="findPageByPlatform" resultMap="TicketAttributePo">
        SELECT <include refid="TicketAttributeColumns"/>
        FROM ticket_attribute
        WHERE <include refid="platformScopeWhere"/>
        ORDER BY sort_order ASC, id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findPageByDomain" resultMap="TicketAttributePo">
        SELECT <include refid="TicketAttributeColumns"/>
        FROM ticket_attribute
        WHERE <include refid="domainScopeWhere"/>
        ORDER BY sort_order ASC, id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findById" resultMap="TicketAttributePo">
        SELECT <include refid="TicketAttributeColumns"/>
        FROM ticket_attribute
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="findPlatformByName" resultMap="TicketAttributePo">
        SELECT <include refid="TicketAttributeColumns"/>
        FROM ticket_attribute
        WHERE scope = 'platform' AND name = #{name}
        LIMIT 1
    </select>

    <select id="findPlatformBySystemKey" resultMap="TicketAttributePo">
        SELECT <include refid="TicketAttributeColumns"/>
        FROM ticket_attribute
        WHERE scope = 'platform'
          AND business_domain_id IS NULL
          AND system_key = #{systemKey}
        LIMIT 1
    </select>

    <select id="findDomainByName" resultMap="TicketAttributePo">
        SELECT <include refid="TicketAttributeColumns"/>
        FROM ticket_attribute
        WHERE scope = 'domain' AND business_domain_id = #{domainId} AND name = #{name}
        LIMIT 1
    </select>

    <select id="findMaxSortOrderPlatform" resultType="java.lang.Integer">
        SELECT MAX(sort_order) FROM ticket_attribute WHERE scope = 'platform'
    </select>

    <select id="findMaxSortOrderDomain" resultType="java.lang.Integer">
        SELECT MAX(sort_order) FROM ticket_attribute
        WHERE scope = 'domain' AND business_domain_id = #{domainId}
    </select>

    <insert id="insert" parameterType="com.uniondesk.ticket.entity.TicketAttributePo"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ticket_attribute (
            scope, business_domain_id, name, description, field_type, type_config,
            status, sort_order, is_system, system_key, source_attribute_id, created_by, updated_by,
            created_at, updated_at
        )
        VALUES (
            #{scope}, #{businessDomainId}, #{name}, #{description}, #{fieldType}, #{typeConfig},
            #{status}, #{sortOrder}, #{system}, #{systemKey}, #{sourceAttributeId}, #{createdBy}, #{updatedBy},
            CURRENT_TIMESTAMP(3), CURRENT_TIMESTAMP(3)
        )
    </insert>

    <update id="update">
        UPDATE ticket_attribute
        SET name = #{name},
            description = #{description},
            field_type = #{fieldType},
            type_config = #{typeConfig},
            status = #{status},
            updated_by = #{updatedBy},
            updated_at = CURRENT_TIMESTAMP(3)
        WHERE id = #{id}
    </update>

    <delete id="deleteByIdPlatform">
        DELETE FROM ticket_attribute
        WHERE id = #{id} AND scope = 'platform'
    </delete>

    <delete id="deleteByIdDomain">
        DELETE FROM ticket_attribute
        WHERE id = #{id} AND scope = 'domain' AND business_domain_id = #{domainId}
    </delete>

    <update id="updateSortOrder">
        UPDATE ticket_attribute
        SET sort_order = #{sortOrder},
            updated_by = #{updatedBy},
            updated_at = CURRENT_TIMESTAMP(3)
        WHERE id = #{id}
    </update>

</mapper>
