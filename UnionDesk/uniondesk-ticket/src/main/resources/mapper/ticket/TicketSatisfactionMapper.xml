<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.ticket.mapper.TicketSatisfactionMapper">

    <insert id="insert" parameterType="com.uniondesk.ticket.entity.TicketSatisfactionPo"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ticket_satisfaction (
            business_domain_id, ticket_id, customer_id, rating, comment, status
        )
        VALUES (
            #{businessDomainId}, #{ticketId}, #{customerId}, #{rating}, #{comment}, 'submitted'
        )
    </insert>

    <select id="findByTicketIdAndDomainId" resultType="com.uniondesk.ticket.entity.TicketSatisfactionPo">
        SELECT
            id,
            business_domain_id,
            ticket_id,
            customer_id,
            rating,
            comment,
            status,
            created_at,
            updated_at
        FROM ticket_satisfaction
        WHERE ticket_id = #{ticketId} AND business_domain_id = #{domainId}
        LIMIT 1
    </select>

</mapper>
