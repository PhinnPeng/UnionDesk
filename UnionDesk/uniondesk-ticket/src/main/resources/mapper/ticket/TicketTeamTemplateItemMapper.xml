<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.ticket.mapper.TicketTeamTemplateItemMapper">

    <resultMap id="TicketTeamTemplateItemPo" type="com.uniondesk.ticket.entity.TicketTeamTemplateItemPo">
        <id property="id" column="id"/>
        <result property="teamTemplateId" column="team_template_id"/>
        <result property="ticketTypeId" column="ticket_type_id"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="includeFormSchema" column="include_form_schema"/>
        <result property="includeWorkflow" column="include_workflow"/>
        <result property="includeDescriptionTemplate" column="include_description_template"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="ticketTypeCode" column="ticket_type_code"/>
        <result property="ticketTypeName" column="ticket_type_name"/>
    </resultMap>

    <sql id="ItemSelect">
        i.id, i.team_template_id, i.ticket_type_id, i.sort_order,
        i.include_form_schema, i.include_workflow, i.include_description_template,
        i.created_at, i.updated_at,
        t.code AS ticket_type_code, t.name AS ticket_type_name
    </sql>

    <select id="findByTemplateId" resultMap="TicketTeamTemplateItemPo">
        SELECT <include refid="ItemSelect"/>
        FROM ticket_team_template_item i
        LEFT JOIN ticket_type t ON t.id = i.ticket_type_id
        WHERE i.team_template_id = #{teamTemplateId}
        ORDER BY i.sort_order ASC, i.id ASC
    </select>

    <select id="findByTemplateIds" resultMap="TicketTeamTemplateItemPo">
        SELECT <include refid="ItemSelect"/>
        FROM ticket_team_template_item i
        LEFT JOIN ticket_type t ON t.id = i.ticket_type_id
        WHERE i.team_template_id IN
        <foreach collection="teamTemplateIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY i.team_template_id ASC, i.sort_order ASC, i.id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ticket_team_template_item (
            team_template_id, ticket_type_id, sort_order,
            include_form_schema, include_workflow, include_description_template
        ) VALUES (
            #{teamTemplateId}, #{ticketTypeId}, #{sortOrder},
            #{includeFormSchema}, #{includeWorkflow}, #{includeDescriptionTemplate}
        )
    </insert>

    <delete id="deleteByTemplateId">
        DELETE FROM ticket_team_template_item
        WHERE team_template_id = #{teamTemplateId}
    </delete>

</mapper>
