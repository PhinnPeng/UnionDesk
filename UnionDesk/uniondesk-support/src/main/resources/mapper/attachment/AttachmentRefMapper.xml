<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.attachment.mapper.AttachmentRefMapper">

    <insert id="insert" parameterType="com.uniondesk.attachment.entity.AttachmentRefPo">
        INSERT INTO attachment_ref (attachment_id, target_type, target_id, relation_type)
        VALUES (#{attachmentId}, #{targetType}, #{targetId}, #{relationType})
    </insert>

    <select id="countLinkedToCustomerTicket" resultType="int">
        SELECT COUNT(*)
        FROM attachment_ref ar
        JOIN ticket t ON t.id = ar.target_id
        WHERE ar.attachment_id = #{attachmentId}
          AND ar.target_type = 'ticket'
          AND t.customer_id = #{customerId}
    </select>

</mapper>
