<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.uniondesk.sla.mapper.SlaTicketMapper">

    <resultMap id="SlaTicketPo" type="com.uniondesk.sla.entity.SlaTicketPo">
        <result property="priority" column="priority"/>
        <result property="slaStatus" column="sla_status"/>
        <result property="slaFirstResponseDeadline" column="sla_first_response_deadline"/>
        <result property="slaResolutionDeadline" column="sla_resolution_deadline"/>
        <result property="slaFirstRespondedAt" column="sla_first_responded_at"/>
        <result property="slaResolvedAt" column="sla_resolved_at"/>
        <result property="breachActionJson" column="breach_action_json"/>
        <result property="slaBreachActioned" column="sla_breach_actioned"/>
    </resultMap>

    <select id="selectSlaSnapshot" resultMap="SlaTicketPo">
        SELECT
            t.priority,
            t.sla_status,
            t.sla_first_response_deadline,
            t.sla_resolution_deadline,
            t.sla_first_responded_at,
            t.sla_resolved_at,
            t.sla_breach_actioned,
            COALESCE(sc.breach_action_json, sr.breach_action_json) AS breach_action_json
        FROM ticket t
        LEFT JOIN sla_config sc
          ON sc.business_domain_id = t.business_domain_id
        LEFT JOIN sla_rule sr
          ON sr.business_domain_id = t.business_domain_id
         AND (sr.ticket_type_id = t.ticket_type_id OR sr.ticket_type_id IS NULL)
         AND (sr.priority_level_id IS NULL OR EXISTS (
             SELECT 1
             FROM ticket_priority_level tpl
             WHERE tpl.id = sr.priority_level_id AND tpl.code = t.priority
         ))
        WHERE t.id = #{ticketId} AND t.business_domain_id = #{domainId}
        ORDER BY sr.priority_level_id IS NOT NULL DESC, sr.ticket_type_id IS NOT NULL DESC, sr.id DESC
        LIMIT 1
    </select>

    <select id="selectCreatedAtById" resultType="java.time.LocalDateTime">
        SELECT created_at
        FROM ticket
        WHERE id = #{ticketId} AND business_domain_id = #{domainId}
        LIMIT 1
    </select>

    <update id="updateSlaDeadlines">
        UPDATE ticket
        SET sla_first_response_deadline = #{firstResponseDeadline},
            sla_resolution_deadline = #{resolutionDeadline},
            sla_status = 'tracking',
            sla_paused_duration = COALESCE(sla_paused_duration, 0),
            sla_pause_started_at = NULL
        WHERE id = #{ticketId}
    </update>

    <update id="updateFirstResponse">
        UPDATE ticket
        SET sla_first_responded_at = COALESCE(sla_first_responded_at, #{now}),
            sla_status = COALESCE(sla_status, 'tracking')
        WHERE id = #{ticketId} AND business_domain_id = #{domainId}
    </update>

    <update id="updateResolution">
        UPDATE ticket
        SET sla_resolved_at = COALESCE(sla_resolved_at, #{now}),
            sla_status = 'stopped'
        WHERE id = #{ticketId} AND business_domain_id = #{domainId}
    </update>

    <update id="updatePriorityAndSlaStatus">
        UPDATE ticket
        SET priority = #{priority},
            sla_status = #{slaStatus},
            updated_at = CURRENT_TIMESTAMP(3)
        WHERE id = #{ticketId}
    </update>

    <!-- 违约动作原子认领：受影响行数 = 1 才是动作执行者 -->
    <update id="claimBreachAction">
        UPDATE ticket
        SET sla_breach_actioned = 1,
            updated_at = CURRENT_TIMESTAMP(3)
        WHERE id = #{ticketId} AND sla_breach_actioned = 0
    </update>

    <!-- 仅翻转 SLA 状态（自愈 / 非认领者只翻状态） -->
    <update id="updateSlaStatus">
        UPDATE ticket
        SET sla_status = #{status},
            updated_at = CURRENT_TIMESTAMP(3)
        WHERE id = #{ticketId} AND business_domain_id = #{domainId}
    </update>

</mapper>
