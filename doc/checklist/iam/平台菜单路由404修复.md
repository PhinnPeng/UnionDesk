# 平台菜单路由 404 修复

## 目标
修复点击平台端“角色管理”“菜单管理”时出现 404 的问题，确保后端目录菜单归一后不会丢失子路由，也不会生成重复菜单 key。

## 开发清单
- [x] 复现多个平台目录归一到 `/platform` 后，动态路由去重丢弃后续目录子路由的问题
- [x] 调整动态路由去重逻辑，重复 path 时合并 children
- [x] 修复后端目录归一后菜单渲染重复 key 的问题
- [x] 对齐 `PermissionSnapshotMenu.path` 类型，允许后端目录节点返回 `null`

## 验证
- [x] `pnpm test --run src/router/utils/generate-user-menus.test.ts src/router/utils/generate-routes-from-backend.test.ts`
- [x] `pnpm typecheck`
