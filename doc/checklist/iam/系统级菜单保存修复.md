# 系统级菜单保存修复

## 目标
修复菜单管理中编辑系统级菜单时误报“系统必要按钮不可修改”的问题，允许保存系统级菜单及其必需按钮配置，同时保留结构校验。

## 开发清单
- [x] 复现后端 `required=true` 菜单保存被拦截的问题
- [x] 放开 `AdminMenuService.updateMenu` 对系统级菜单的整体保存拦截
- [x] 保留菜单节点类型、父级、路由、组件、权限码等现有校验
- [x] 补充单元测试与集成测试覆盖系统级菜单保存
- [x] 重启后端并验证 `/api/v1/health` 可用
- [x] 真实接口验证 `PUT /api/v1/iam/menus/{id}` 可保存 `/platform/home`

## 验证
- [x] `mvnw.cmd "-Dtest=AdminMenuServiceTest#updateMenuAllowsRequiredMenu,AdminMenuRequiredMenuIntegrationTest#updateMenuAllowsRequiredPlatformHome" test`
- [x] `/api/v1/health` 返回 `UP`
- [x] `/api/v1/iam/menus/tree?scope=platform` 确认 `/platform/home` 仍为 `required=true`
