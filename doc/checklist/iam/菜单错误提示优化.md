# 菜单错误提示优化清单

## 背景
菜单管理页删除含子节点菜单时，后端已返回明确业务错误信息，但前端同时出现业务错误与通用“请求失败”提示。

## 计划
1. 定位重复提示来源
   - 验证：确认接口错误响应与页面 catch 是否各弹一次。
   - 状态：已完成
2. 调整后端 JSON 请求错误处理
   - 验证：响应体包含错误信息时，只抛出响应错误信息，不再先全局提示。
   - 状态：进行中
3. 回归菜单管理页删除错误提示
   - 验证：删除含子节点菜单时仅显示后端错误信息。
   - 状态：待验证

## 检查点
- `pnpm --dir UnionDeskWeb/apps/UnionDeskAdminWeb test src/api/backend.test.ts --run` 通过
- `pnpm --dir UnionDeskWeb/apps/UnionDeskAdminWeb typecheck` 通过
