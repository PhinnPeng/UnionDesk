# 角色管理对接与菜单数据修复

## 目标

1. 修复平台菜单树中"权限管理→角色管理/菜单管理"的父子关系，补全缺失的 CRUD 按钮节点
2. 角色管理页面 (`/platform/role`) 从 mock API 切换到真实后端 API
3. 角色管理支持 scope 字段展示与编辑

## 开发清单

### 1. DB 迁移：菜单数据修复

- [x] 确保 角色管理(`/platform/role`) 和 菜单管理(`/platform/menu`) 的 `parent_id` 指向 权限管理(`/platform/permission`)
- [x] 补全 角色管理 下的 CRUD 按钮节点 (`platform.role.create/update/delete/permission_read/permission_update/bind`)
- [x] 补全 菜单管理 下的 CRUD 按钮节点 (`platform.menu.create/update/delete`)
- [x] 授权 super_admin 拥有新增的按钮节点
- 迁移文件：`V202605110001__fix_platform_permission_tree_and_buttons.sql`

### 2. 前端：角色管理 API 层重写

- [x] 重写 `api/system/role/index.ts`，调用后端 `/api/v1/iam/roles` 系列接口
- [x] 重写 `api/system/role/types.ts`，对齐后端 `RoleView` 数据结构 (id, code, name, scope, system)
- [x] 新增角色权限 API：`GET/PUT /api/v1/iam/roles/{id}/permissions`
- [x] 菜单树 API 复用：`GET /api/v1/iam/menus/tree`（通过 `fetchMenuTree`）

### 3. 前端：角色管理页面改造

- [x] `pages/system/role/index.tsx` 对接真实 API（列表、删除）
- [x] `pages/system/role/constants.tsx` 新增 scope 列、system 列，移除 status/remark/时间字段
- [x] `pages/system/role/components/detail.tsx` 对接真实 API（新增/编辑/菜单权限分配）
- [x] 菜单权限树从 mock `role-menu` 切换到 `GET /api/v1/iam/menus/tree`
- [x] 角色权限分配使用 `GET/PUT /api/v1/iam/roles/{id}/permissions`
- [x] i18n 新增 scope/scopeGlobal/scopeDomain/systemRole/assignPermission 翻译

## 验证

- 菜单管理页 (`/platform/menu`) scope=platform 时，权限管理 可展开包含 角色管理 和 菜单管理
- 角色管理页列表显示真实后端角色数据，包含 scope 列
- 新增/编辑角色可选 scope，可分配菜单权限
- 删除角色调用后端接口

## 2026-05-12 角色保存反馈与刷新

- [x] 角色抽屉保存成功后使用 Ant Design `App.useApp().message` 显示成功提示，避免依赖可选全局 `window.$message`
- [x] 保存成功后等待角色列表刷新回调执行，确保新增/编辑结果回写到列表
- [x] 补充角色抽屉保存测试，覆盖成功提示、关闭抽屉、刷新列表
