# 菜单栏权限渲染修复清单

## 目标
将 AdminWeb 的菜单栏渲染从“静态路由合并”切换为“后端返回菜单目录驱动”，并保持路由注册链路完整。

## 任务
1. 新增独立菜单生成模块 `src/router/utils/generate-user-menus.ts`
2. 重构 `src/store/access.ts`，拆分 `userRoutes`、`userMenus`、`platformMenus` 与 `routeList`
3. 更新 `src/router/guard/auth-guard.tsx`，分离后端菜单与路由注册输入
4. 更新 `src/layout/layout-menu/use-menu.ts` 和全局搜索菜单来源
5. 补充单元测试与回归验证

## 状态
- [x] 已完成

## 验证
- [x] 仅后端返回的菜单显示在侧边栏
- [x] 无权限菜单不显示
- [x] 路由跳转功能正常
- [x] TypeScript 检查通过
- [x] 相关测试通过

## 结果
- 已新增 `generate-user-menus.ts`，菜单生成与静态路由注册逻辑解耦。
- 已将 `access.ts` 切换为 `userRoutes` / `userMenus` / `platformMenus` / `routeList` 的分离状态。
- 已同步 `auth-guard`、`use-menu` 与全局搜索的数据来源。
- 已通过 `pnpm --dir "F:/WorkSpace/UnionDesk/UnionDeskWeb/apps/UnionDeskAdminWeb" typecheck`
- 已通过 `pnpm --dir "F:/WorkSpace/UnionDesk/UnionDeskWeb/apps/UnionDeskAdminWeb" test --run`
- 已执行 `mvn -f UnionDesk/pom.xml "-Dflyway.outOfOrder=true" flyway:migrate`
- 已验证平台菜单中 `audit-logs`、`domain-config`、`sla-management`、`system-settings` 为 `hidden=0`
- 已验证 `ticket-detail` 仍保持 `hidden=1`
- 已通过 `mvn -f UnionDesk/pom.xml "-Dtest=AdminMenuServiceTest,IamControllerTest" test`

## 2026-05-09 后端补充
- [x] 新增 `V202605090003__admin_menu_role_minimal.sql`，收敛 `super_admin` 菜单授权到最小集，并清理 `admin` 的业务域角色
- [x] `LoginSessionService.validateAndTouch` 已改为刷新 `expires_at`，形成滑动窗口
- [x] 新增 `LoginSessionServiceTest`，覆盖 session 续期行为
- [x] 补充 `IamControllerTest`，覆盖 `permission-snapshot` 仅返回目标菜单与 `super_admin`
## 2026-05-09 刷新登录态与角色最小化
- [x] 修复 `auth-guard` 刷新后回写 `userStore`
- [x] 统一 `refresh-token` 调用路径到 `/api/v1/auth/refresh`
- [x] `goLogin` 兼容 history / hash 两种路由模式
- [x] 新增 `V202605090003__admin_menu_role_minimal.sql`
- [x] `LoginSessionService` 刷新 `expires_at` 形成滑动窗口
- [x] 补充前后端单测并准备执行 `typecheck` / `test` / `mvn test`

## 2026-05-09 super_admin CRUD 按钮补齐
- [x] 新增 `V202605090004__fix_super_admin_crud_buttons.sql`
- [x] 补充真实数据库集成测试，验证 `permission-snapshot.actions` 包含 CRUD 权限码
- [x] 收口 `auth-guard` 的重复快照拉取分支
- [x] 提取登录相关白名单常量，统一前后端刷新路径语义

## 2026-05-09 菜单管理 API 数据一致性修复
- [x] `AdminMenuService.listGrantedMenuTree`: 返回扁平列表（与 permission-snapshot 一致）
- [x] `AdminMenuService.PermissionSnapshotData`: 新增 `menuTree` 字段
- [x] `IamController.listMenusTree`: 在后端构建树形结构后返回
- [x] `types.ts`: 前端 `PermissionSnapshot` 添加 `menuTree` 字段
- [x] `utils.ts`: 前端使用后端返回的 `menuTree` 构建路由

## 2026-05-09 菜单管理页 UX 优化（第五轮修复）
- [x] `index.tsx`: 操作栏按钮包裹 `flex justify-center gap-2` 容器实现整体居中

## 2026-05-09 /platform/home 404 修复
- [x] `V202605090005__platform_home_required.sql`：`/platform/home` 标记为 `required=1` 并授予 `super_admin`
- [x] `replaceRolePermissions` 自动携带 `required` 菜单与其 `required` 按钮
- [x] `updateMenu` 保护 `required` 菜单不可修改
- [x] 前端菜单管理表格隐藏 `required` 行的编辑/删除按钮，并标记为 `系统`

## 2026-05-10 菜单管理 API 数据一致性与树形结构修复
- [x] `AdminMenuService.listGrantedMenusOnly`：按 scope 返回与 `permission-snapshot` 一致的菜单数据
- [x] `AdminMenuService.listGrantedMenusByScope`：返回 `platform/business` 两个 scope 的菜单集合
- [x] `IamController.listMenusTree`：按 `scope` 返回数组，不带 `scope` 返回 scope map
- [x] `IamService.PermissionSnapshot` / `IamController.currentPermissionSnapshot`：暴露 `menuTree`
- [x] `PermissionSnapshotMenu` / `buildBackendRoutesFromSnapshot`：兼容树形与平铺两种输入
- [x] 集成测试与单元测试：覆盖菜单树 scope、scope map、`menuTree` 回归

## 2026-05-10 菜单快照与菜单管理树分流修复
- [x] `permission-snapshot` 移除 `menus`，仅保留 `menuTree + actions`
- [x] `/api/v1/iam/menus/tree` 返回完整树形菜单并保留按钮节点
- [x] `super_admin` 恢复平台域菜单关系，确保快照菜单树与管理树一致
- [x] `replaceRolePermissions` 自动携带 required 菜单与按钮
- [x] `updateMenu` 保护 required 菜单不可修改
- [x] 前端 `PermissionSnapshot` 与用户路由构建仅消费 `menuTree`
## 2026-05-10 菜单快照与菜单管理树分流修复
- [x] `permission-snapshot` 移除 `menus`，仅保留 `menuTree + actions`
- [x] `/api/v1/iam/menus/tree` 返回完整树形菜单并保留 `button` 节点，`scope` 透传保持一致
- [x] `super_admin` 平台域菜单关系回补，确保快照与菜单管理树一致
- [x] `replaceRolePermissions` 自动携带 `required` 菜单与按钮
- [x] `updateMenu` 保护 `required` 菜单不可修改
- [x] 前端 `PermissionSnapshot` 与路由构建仅消费 `menuTree`
- [x] 验证通过：`mvnw.cmd -Dtest=AdminMenuServiceTest,IamControllerTest,AdminMenuRequiredMenuIntegrationTest,IamServiceTests test`
- [x] 验证通过：`pnpm typecheck`
- [x] 验证通过：`pnpm test --run`

## 2026-05-10 菜单子节点不可见修复
- [x] 重启后端加载最新菜单树实现，避免旧进程继续返回空 `children`
- [x] 新增 `V202605110001__fix_platform_permission_tree_and_buttons.sql`，补齐平台菜单树树结构与缺失按钮节点
- [x] 真实登录验证 `GET /api/v1/iam/menus/tree?scope=platform`，确认 `41` 可递归带出子节点 `54`
- [x] 真实登录验证 `GET /api/v1/iam/menus/tree`，确认仍返回 `platform/business` 两棵树
- [x] 菜单页手工验证前提完成，后续只需在前端展开 `platform/ticket-detail` 即可看到按钮节点

## 2026-05-10 菜单绑定角色时允许删除
- [x] `AdminMenuService.deleteMenu`：删除前先清理 `iam_admin_role_menu_relation`，再删除菜单本体
- [x] 保留子节点保护：存在子节点时仍禁止删除，避免破坏树结构
- [x] 单测验证：绑定角色的菜单可删除，且对应角色菜单关系会被同步删除
