# 菜单更新 500 修复清单

## 目标
修复 `PUT /api/v1/iam/menus/38` 更新菜单时触发数据库约束并返回 500 的问题，改为在业务层返回明确的中文校验错误。

## 开发清单
- [x] 读取后端日志，确认根因是 `chk_iam_admin_menu_catalog_fields` 约束被违反
- [x] 在 `AdminMenuService.updateMenu` 增加 `required=true` 节点类型变更拦截
- [x] 补充单元测试覆盖 `required` 节点改为 `catalog` 的失败场景
- [x] 重启后端并验证服务可用
- [x] 真实接口验证 `PUT /api/v1/iam/menus/38` 不再返回 500
