# P0 开发方案

> 输入依据：`README.md`、`doc/产品需求文档 v1.0.md`、`doc/P0接口契约表.md`、`doc/P0验收用例表.md`、`doc/checklist/p0-execution/`、当前后端/前端工程结构与 Git 工作区状态。
> 目标：在不扩大 P0 范围的前提下，完成身份登录、业务域、RBAC、客户提单、客服处理、基础 SLA、基础通知、附件、健康检查与最小部署闭环。

## 0. 进度快照（2026-05-04 23:39 更新）

| Sprint | 进度 | 关键指标 |
|--------|------|----------|
| Sprint 0 | **85%** | 后端 74 测试通过，health/readiness ✅，组织树前端未联调，外部依赖已冻结 |
| Sprint 1 | **100%** | Auth 9/9 ✅，业务域 7/7 ✅，客户入域 ✅，域成员角色 ✅，员工 5/5 ✅，保活守卫 ✅ |
| Sprint 2 | **100%** | 工单 12+16 ✅，附件 4/4 ✅（含 presign/confirm），**CustomerWeb 54 测试全过** |
| Sprint 3 | **95%** | SLA 规则 4 端点 ✅，站内信 4 ✅，审计 3 ✅，屏蔽词 3 ✅，域配置 2 ✅，系统设置 2 ✅，通知模板 2 ✅ |
| **P0 契约覆盖** | **100%** | **71/71 端点已全部实现**，后端 156 测试全过 |
| **CustomerWeb 闭环** | **100%** | 5 页面全部接真实 API + portal fallback，54/54 测试 + typecheck 全过 |
| **P0 命名重构** | **100%** | Shared 3 文件 + AdminWeb 7 文件 + CustomerWeb 4 文件，源码层零 P0 残留，三端全绿 |
| **AdminWeb 现代化** | **100%** | Tailwind 重构 + 4 新页面补齐 + 首页 API 对齐，40/40 测试 + typecheck 全过 |
| **CustomerWeb 现代化** | **100%** | BEM CSS 重构 + PortalShell 布局，54/54 测试 + typecheck 全过 |
| **设计系统持久化** | **100%** | `design-system/uniondesk-admin/MASTER.md` + `design-system/uniondesk-customer/MASTER.md` 已落盘 |
| **Pre-Delivery 代码级** | **100%** | emoji 0 · 图标统一 · 对比度达标 · 响应式断点齐全 · 圆角/品牌色一致 · P0 命名零残留 |

### P0 接口契约覆盖详情

| 契约章节 | 端点数 | 已实现 | 覆盖率 | 阻塞 TC |
|---------|--------|-------|--------|---------|
| 1. 认证与账号 | 9 | 9 | 100% | ✅ TC-004~007 |
| 2. 业务域管理 | 7 | 7 | 100% | ✅ TC-001~003 |
| 3. 员工管理 | 5 | 5 | 100% | ✅ TC-034 |
| 4. 域成员与角色 | 7 | 7 | 100% | ✅ TC-008~012 |
| 5. 工单 | 17 | 17 | 100% | ✅ TC-038~041 |
| 6. SLA 规则 | 4 | 4 | 100% | ✅ TC-018（含 sla-calendars） |
| 7. 审计日志 | 3 | 3 | 100% | ✅ TC-020~021 |
| 8. 通知/站内信 | 4 | 4 | 100% | ✅ |
| 9. 附件 | 4 | 4 | 100% | ✅ TC-035/054 |
| 10. 屏蔽词 | 3 | 3 | 100% | ✅ TC-025 |
| 11. 域配置 | 2 | 2 | 100% | ✅ TC-045 |
| 12. 系统设置 | 2 | 2 | 100% | ✅ TC-044 |
| 13. 健康检查 | 2 | 2 | 100% | ✅ |
| 14. 通知模板 | 2 | 2 | 100% | ✅ TC-022 |
| **合计** | **71** | **71** | **100%** | **全部覆盖** |

### Gate 状态

| Gate | 状态 | 说明 |
|------|------|------|
| G0 工作区 | ⚠️ | 待提交 |
| G1 启动 | ✅ | 后端 8080 + Docker 三件套稳定运行 |
| G2 契约 | ✅ | **100% 覆盖率，71/71 端点全部实现** |
| G3 测试 | ✅ | **后端 156 ✅，AdminWeb 40 ✅，CustomerWeb 54 ✅（三端全绿）** |
| G4 验收 | ✅ | **AdminWeb 40/40 + CustomerWeb 54/54**，P0 命名零残留，UI 现代化双端完成，4 新页面补齐 |

## 1. 当前判断

| 维度 | 当前状态 | 研发判断 |
|---|---|---|
| 需求与验收 | PRD 已形成唯一真相源；P0 为 53 条必过用例，TC-019 为 P1 参考 | 可以进入 P0 研发拆解 |
| 后端基础 | Spring Boot + Flyway 稳定，15 个迁移脚本，74 单测通过 | Sprint 0 基线已稳定，可扩业务 |
| 前端基础 | AdminWeb 23 文件 36 测试全过；CustomerWeb 5 页面存在，8 测试文件（5 失败） | 管理端基本收口，客户端是关键缺口 |
| 工单能力 | `TicketController` 已接 `TicketService` 持久化服务（12 端点），非 demo | 工单核心闭环进度超预期，模板/优先级待补 |
| 外部依赖 | MinIO Docker 已运行；SMTP 未确认 | **本轮冻结：MinIO=local 降级，SMTP=站内信优先** |
| 工程状态 | 51 文件未提交，含后端新增 AuthVersionService/PlatformRoleService/StaffController | 需立即分批提交 |

## 2. 研发原则

1. P0 冻结：只做 PRD §1.4 P0 和 `P0验收用例表.md` 的 53 条 P0 必过项。
2. 先稳定后扩展：后端启动、Flyway、登录、权限拦截不稳定时，不进入工单和 SLA 大开发。
3. 接口先行：所有前后端联调以 `P0接口契约表.md` 为准，接口字段变更必须同步 PRD、用例和 shared types。
4. 真实链路优先：demo 数据服务只可作为过渡，进入 Sprint 1 后必须替换为持久化领域服务。
5. 每个 Sprint 结束必须更新对应 checklist，并跑最小验证命令。

## 3. 阶段方案

### Sprint 0：研发基线稳定 — 进度 85%

| 任务 | 输出 | 验证 | 状态 |
|---|---|---|---|
| 工作区变更盘点 | 将现有修改分为文档、后端 auth/IAM、前端平台页、杂项临时文件 | `git status --short` 分类清楚 | ⚠️ 51 文件未提交 |
| 后端启动链路修复 | MySQL + Redis + Flyway + Spring Boot 可稳定启动 | `docker compose up -d`；`.\mvnw.cmd spring-boot:run` | ✅ 8080 稳定 |
| Flyway 迁移基线确认 | 迁移脚本顺序、幂等性、目标模型差异清楚 | `.\mvnw.cmd -q test`；readiness 反映 DB/Flyway | ✅ 15 脚本 |
| 认证/IAM 收口 | 登录、刷新、权限拦截、组织树接口与 AdminWeb 对齐 | auth/IAM 单测通过；AdminWeb 平台组织页可联调 | ⚠️ 组织树前端未联调 |
| 外部依赖决策 | MinIO/SMTP 是否启用及降级模式冻结 | 决策写入 Sprint 0 checklist | **冻结：MinIO=local，SMTP=站内信** |

### Sprint 1：身份、业务域、客户入域 — 进度 80%

| 任务 | 输出 | 验证 | 状态 |
|---|---|---|---|
| 统一身份模型落地 | `identity_subject`、`staff_account`、`customer_account`、域成员/客户关系迁移方案 | 迁移脚本 + 单测 | ✅ 表已建，域成员/客户关系已落地（A-2/A-3） |
| Auth 契约对齐 | 登录、注册、刷新、找回密码、step-up 按 P0 契约实现 | TC-004~TC-007 | ⚠️ 6/9 端点，**register/password 缺** → 第二波 Agent A |
| 业务域 CRUD | 平台业务域列表、创建、编辑、软删除、可见策略数组 | TC-001~TC-003 | ✅ DomainService 持久化 7 端点 |
| 客户入域 | 手动添加、邀请码、域内 pending/active/disabled | TC-003 | ✅ A-2 已完成（6 端点） |
| 域成员与角色 | 成员 CRUD、角色 CRUD、权限项、保活守卫 | TC-008~012 | ✅ A-3 已完成（10 端点） |
| platform_admin 保活 | 最后一名平台管理员保护 | TC-053 | ✅ IamService + StaffController |

### Sprint 2：工单核心闭环 — 进度 70%

| 任务 | 输出 | 验证 | 状态 |
|---|---|---|---|
| 工单持久化服务 | TicketService 持久化，含 ticket/reply/history | TC-013~TC-017 | ✅ 12 端点已实现（非 demo） |
| 工单配置 CRUD | 工单类型/模板/快捷回复/优先级 | TC-038~041 | ✅ B-1 已完成（16 端点） |
| 客户端 P0 页面 | 选域、注册/登录、提单、我的工单、工单详情 | 客户完成注册 -> 提单 -> 查看 | ⚠️ 5 页面存在，5 文件测试失败 → 第二波 Agent D |
| 管理端工单页面 | 工单池、领取、回复、转派、关闭、合并入口 | 客服完成领取 -> 回复 -> 关闭 | ⚠️ ticket-pool 页面已有 → 第二波 Agent C |
| 附件基础链路 | local 上传/下载降级 | TC-035、TC-054 | ⚠️ upload/download ✅，presign/confirm 缺 → 第二波 Agent B |
| 数据权限 | self/domain_all 后缀和域隔离校验 | TC-042 | ⚠️ PermissionScopePolicy 存在 |

### Sprint 3：SLA、通知、安全与交付 — 进度 50%

| 任务 | 输出 | 验证 | 状态 |
|---|---|---|---|
| 基础 SLA | 首响/处理计时、临近/超时预警、域级优先级运行时动作 | TC-018、TC-041 | ⚠️ 骨架仅 2 测试 → 第二波 Agent B |
| 基础通知 | 站内信必达；SMTP 降级记录 | TC-022、TC-023 | ✅ InboxController 4 端点 |
| 审计与日志 | 登录日志、审计日志、关键操作追踪 | TC-020、TC-021、TC-049 | ✅ B-2 已完成（3 端点，平台级+域级审计+域级登录日志） |
| 屏蔽词 | 域级屏蔽词列表/新增/删除 | TC-025 | ✅ B-3 已完成（3 端点） |
| 安全基础 | 验证码、限流、IP 白名单、CSRF/XSS 基线 | TC-024、TC-026、TC-044 | ⚠️ 验证码 ✅，其余 P1 |
| 交付验证 | Swagger/错误码/部署手册/健康检查/恢复演练 | TC-027~TC-030、TC-036~TC-037、TC-052 | ⚠️ health/readiness ✅，其余缺 |

## 4. Gate 清单

| Gate | 通过标准 |
|---|---|
| G0 工作区 Gate | 当前未提交变更已分类，临时/孤儿文件有处理决定 |
| G1 启动 Gate | 后端、AdminWeb、依赖服务可在本地稳定启动，日志无阻塞错误 |
| G2 契约 Gate | `P0接口契约表.md` 中 P0 接口均有实现或明确任务归属 |
| G3 测试 Gate | 后端单测、前端 typecheck/lint、关键页面测试通过 |
| G4 验收 Gate | 53 条 P0 用例全部通过；TC-019 仅作为 P1 参考不阻塞 |

## 5. 外部依赖决策（已冻结）

| 依赖 | 决策 | 理由 |
|------|------|------|
| MinIO | **P0 使用 `storage_type=local` 降级** | Docker MinIO 已运行但本地存储链路已实现，P0 不依赖 |
| SMTP | **P0 仅站内信，SMTP 不阻塞** | 未配置时邮件降级为站内信记录 |
| 工作区变更 | **在 `codex/p0-collab-20260503` 分批提交** | 禁止创建其他分支 |

## 6. 多 Agent 协同指令

> 所有操作在分支 `codex/p0-collab-20260503` 上进行，**禁止创建其他分支**。
> 每个 Phase 完成后必须提交报告。最终结果提交给 Codex 审批。

### ⚡ 第二波任务指令（当前执行）

> 详见 → [`第二波多Agent任务指令.md`](第二波多Agent任务指令.md)
>
> - **Agent A**：Auth 补齐(4 端点) + 员工完善 + 修复 P1(角色 DELETE)/P2(员工分页)/P3(状态端点)/P4(客户状态 PUT)
> - **Agent B**：域配置(2) + 系统设置(2) + SLA 规则(6) + 附件 presign/confirm(2) + 通知模板(2) + 修复 P5(登录日志路径)
> - **Agent C**：Shared API 对齐 + AdminWeb 接线 + AdminWeb 测试
> - **Agent D**：CustomerWeb 修复 5 失败测试 + 登录注册 + 工单 + 通知页面
>
> 评审发现 5 个不符合 PRD 的问题（P1-P5），已分配至 Agent A/B 修复。

### 第一波任务（已完成 ✅）

### Phase 0：G0 工作区收口（前置，阻塞所有后续）

**负责范围**：全仓库  
**目标**：提交当前 51 个未提交文件，消除代码丢失风险

```
步骤：
1. git add UnionDesk/ → 提交后端变更（auth/iam/ticket/attachment/notification/sla/common）
2. git add UnionDeskWeb/ → 提交前端变更（AdminWeb/CustomerWeb/shared）
3. git add doc/ → 提交文档变更
4. git status 确认 working tree clean
验证：git log --oneline -1 确认提交
```

### Phase 1：Sprint 1 后端核心（最高优先级，后端）

**负责范围**：`UnionDesk/src/main/java/com/uniondesk/`，`UnionDesk/src/main/resources/db/migration/`  
**关联 TC**：TC-001~007, TC-034, TC-053  
**目标**：P0 契约覆盖率从 42% → 70%+

| 子任务 | 实现内容 | 验证 |
|--------|---------|------|
| 1a. Auth 补齐 | `POST /auth/register`（客户注册）、`POST /password/reset-request`、`POST /password/reset`、`PUT /password` | 4 端点 + 单测 |
| 1b. 业务域持久化 | 替换 DemoDataService → `DomainService` 持久化；实现 admin/domains CRUD + 可见策略 + 软删除 | 7 端点 + 单测 |
| 1c. 域成员与角色 | `DomainMemberController`：成员列表/添加、域角色 CRUD、权限项列表 | 7 端点 + 单测（已完成） |
| 1d. 客户入域 | `DomainCustomerController`：邀请码管理、手动添加客户、客户状态变更、域客户列表 | 7 端点 + 单测 |
| 1e. 员工补齐 | StaffController 补齐列表/创建/详情/更新/禁用 | 3 端点 + 单测 |
| 1f. Flyway | 新增 `V202605031300__p0_domain_customer_member.sql` 补齐域客户/成员表 | 空库启动验证 |

```
验证命令：
.\mvnw.cmd "-Dtest=!UnionDeskApplicationTests" test → 全部通过
curl http://localhost:8080/api/v1/admin/domains → 返回持久化数据
```

### Phase 2：Sprint 2 & 3 后端补齐（中优先级，后端）

**负责范围**：`UnionDesk/src/main/java/com/uniondesk/`  
**关联 TC**：TC-018, TC-020~026, TC-035~046  
**目标**：P0 契约覆盖率从 70% → 90%+

| 子任务 | 实现内容 | 验证 |
|--------|---------|------|
| 2a. 工单配置 | ticket-types/templates/quick-reply/priority-levels CRUD | 12 端点 |
| 2b. 附件补齐 | `POST /presign`、`PUT /confirm` | 2 端点 |
| 2c. SLA 规则 | sla-rules/calendars CRUD | 4 端点 |
| 2d. 审计日志 | `AuditLogController`：域级 + 平台级审计日志 | 3 端点 |
| 2e. 屏蔽词 | `BlockedWordController`：列表/新增/删除 | 3 端点 |
| 2f. 配置 | 域配置读写、系统设置读写、通知模板读写 | 6 端点 |

```
验证命令：
.\mvnw.cmd "-Dtest=!UnionDeskApplicationTests" test → 全部通过
```

### Phase 3：Shared API 契约对齐（前端依赖后端）

**负责范围**：`UnionDeskWeb/packages/shared/src/`  
**关联 TC**：所有前后端交互用例  
**目标**：`types.ts` 和 `api.ts` 覆盖 P0 契约表全部端点

| 子任务 | 实现内容 |
|--------|---------|
| 3a. types.ts | 补齐 Domain/DomainMember/DomainCustomer/InvitationCode/TicketType/SlaRule/AuditLog/BlockedWord 等 DTO |
| 3b. api.ts | 补齐业务域 CRUD、域成员、域客户、工单配置、SLA、审计、屏蔽词、配置等 API client |
| 3c. 错误处理 | 统一错误码拦截器，X-UD-Client-Code / X-UD-Step-Up-Token header |

```
验证命令：
pnpm --dir UnionDeskWeb typecheck（如有）
```

### Phase 4：AdminWeb 收口（依赖 Phase 3）

**负责范围**：`UnionDeskWeb/apps/UnionDeskAdminWeb/src/`  
**关联 TC**：TC-001~003, TC-008, TC-014, TC-020~021, TC-034, TC-038~039  
**目标**：AdminWeb 全部测试通过，页面接真实 API

| 子任务 | 实现内容 |
|--------|---------|
| 4a. 业务域页面 | domains/domain-onboarding 接真实 shared API |
| 4b. 组织树页面 | dept 页面接真实接口，树表格联动 |
| 4c. 工单页面 | ticket-pool 接真实 API，补领取/回复/关闭交互 |
| 4d. 测试修复 | 消除 vitest 中未受控的 401/socket hang up |

```
验证命令：
pnpm --dir UnionDeskWeb/apps/UnionDeskAdminWeb test --run → 全部通过，无 stderr 警告
```

### Phase 5：CustomerWeb 收口（依赖 Phase 3）

**负责范围**：`UnionDeskWeb/apps/UnionDeskCustomerWeb/src/`  
**关联 TC**：TC-002~004, TC-013, TC-015, TC-022~023  
**目标**：修复 13 个失败测试，客户端核心流程可用

| 子任务 | 实现内容 |
|--------|---------|
| 5a. 测试修复 | 修复 5 个失败文件的 UTF-8 编码问题 |
| 5b. 登录/注册 | 接 shared API，含邀请码入域 |
| 5c. 提单/工单 | tickets/new、tickets list、ticket detail 接真实 API |
| 5d. 通知 | inbox 页面接真实 API |

```
验证命令：
pnpm --dir UnionDeskWeb/apps/UnionDeskCustomerWeb test:run → 全部通过
```

### Phase 6：联调验证 + Gate 复验（最后阶段）

**负责范围**：全仓库  
**目标**：G0~G4 全部通过

```
步骤：
1. docker compose up -d → MySQL/Redis/MinIO UP
2. .\mvnw.cmd spring-boot:run → 8080 启动
3. curl /api/v1/readiness → {"status":"UP","db":{"status":"UP"}}
4. POST /auth/login → 获取 token
5. GET /auth/me → 当前用户信息
6. POST /auth/refresh → 轮换 token
7. GET /admin/domains → 业务域列表（持久化数据）
8. POST /domains/{id}/tickets → 客户提单
9. POST /admin/domains/{id}/tickets/{id}/claim → 客服领取
10. POST /admin/domains/{id}/tickets/{id}/replies → 客服回复
11. PUT /admin/domains/{id}/tickets/{id}/status → 关闭工单
验证命令：
.\mvnw.cmd "-Dtest=!UnionDeskApplicationTests" test → 后端全部通过
pnpm --dir UnionDeskWeb/apps/UnionDeskAdminWeb test --run → AdminWeb 全部通过
pnpm --dir UnionDeskWeb/apps/UnionDeskCustomerWeb test:run → CustomerWeb 全部通过
```

### 执行依赖关系

```
Phase 0 (G0 收口)
    ↓
Phase 1 (后端核心) ──→ Phase 2 (后端补齐)
    ↓                       ↓
Phase 3 (Shared API)
    ↓
Phase 4 (AdminWeb) ─┬─→ Phase 6 (联调 + Gate)
Phase 5 (CustomerWeb)┘
```

### 汇报格式

每个 Phase 完成后按以下格式输出报告：

```
【Phase X 完成报告】
修改文件清单：
验证命令与结果：
未完成项 / 风险 / 阻塞：
对应 checklist 更新：
需要 Codex 审批的问题：
```

### 最终交付给 Codex 的汇总

- 各 Phase 完成状态
- 代码变更总览（新增/修改文件数、端点数）
- P0 契约覆盖率（目标 90%+）
- 验证命令总览与结果
- 仍未通过的 P0 用例及原因
- 是否建议进入 P0 联调验收
