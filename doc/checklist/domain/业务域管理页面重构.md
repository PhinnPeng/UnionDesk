# 业务域管理页面重构开发清单

## 1. 命名重构 (Shared)
- [x] `packages/shared/src/types.ts`
    - [x] `P0AdminDomain` → `AdminDomain`
    - [x] `P0CreateAdminDomainPayload` → `CreateAdminDomainPayload`
    - [x] `P0UpdateAdminDomainPayload` → `UpdateAdminDomainPayload`
    - [x] `AdminDomain` 增加字段：`created_by`, `updated_by`, `creator_name`, `updater_name`, `updated_at`
- [x] `packages/shared/src/api.ts`
    - [x] `fetchP0AdminDomainsPage` → `fetchAdminDomainsPage`
    - [x] `createP0AdminDomain` → `createAdminDomain`
    - [x] `updateP0AdminDomain` → `updateAdminDomain`
    - [x] `deleteP0AdminDomain` → `deleteAdminDomain`
    - [x] API 参数补齐 `created_from?`, `created_to?`

## 2. 引用更新 (Frontend)
- [x] `pages/platform/domains/index.tsx`
- [x] `pages/platform/ticket-pool/index.tsx`
- [x] `pages/platform/attachments/index.tsx`
- [x] `pages/platform/domain-onboarding/index.tsx`

## 3. 后端改造 (Java)
- [x] Flyway 迁移
    - [x] `V202605200003__add_audit_fields_to_business_domain.sql`: 新增 `created_by`, `updated_by` 及索引
    - [x] `V202605200004__restructure_domain_menu.sql`: 新增 catalog，调整 `ADM0000000043`
- [x] `DomainDtos.java`
    - [x] `DomainView` 增加 `created_by`, `updated_by`, `creator_name`, `updater_name`
- [x] `DomainService.java`
    - [x] `listAdminDomains` 加入 `user_account` JOIN 获取名称
    - [x] `listAdminDomains` 支持 `created_from`, `created_to` 筛选
    - [x] `createDomain` 写入 `created_by`
    - [x] `updateDomain` 写入 `updated_by`
    - [x] 补齐 `audit_log` 写入逻辑
- [x] `DomainController.java`
    - [x] `listAdmin` 增加参数支持

## 4. 前端页面重构
- [x] `pages/platform/domains/index.tsx`
    - [x] 引入 `Card`, `Input.Search`, `RangePicker` 筛选区
    - [x] 实现卡片列表视图 (Card Grid)
    - [x] 补齐操作栏：管理、启用/禁用
    - [x] 接入分页逻辑

## 5. 验证
- [ ] 后端编译 & Flyway 执行
- [ ] 前端 `pnpm typecheck`
- [ ] 页面功能点测试
