# 通知偏好与满意度评价清单

## 业务域
- 客户

## 目标
- 将客户端通知偏好设置和满意度评价收敛为统一可执行口径，覆盖可配置通知类别、评价触发、未评价处理、数据模型与统计展示。

## 开发任务
- [x] 在 `3.9 通知中心` 中补充客户通知偏好口径，明确类别级配置、渠道可选项和账户安全类通知不可关闭。
- [x] 在 `3.5.2 会话流程` 中补充关闭会话后的满意度评价邀请与默认 7 天游标。
- [x] 在 `3.5.3 数据与安全` 中补充未评价提醒一次、到期失效且不影响正常服务的机制。
- [x] 在 `2.3 身份与权限数据简表` 中补充 `customer_notification_preference` 与 `satisfaction_rating`。
- [x] 在 `5. 数据模型（核心实体）` 中补充 `satisfaction_rating` 与 `customer_notification_preference` 的核心字段。
- [x] 补充 `settings/notification-preferences` 页面到通知偏好业务域，前端已采用本地回退保存；后端接口待 Epsilon 补齐后再切换真实 API。

## 验证记录
- [x] 客户可按通知类别设置接收偏好，账户安全类通知始终开启。
- [x] 工单关闭和咨询结束后可自动生成评价邀请，未评价会在到期后失效。
- [x] 评价数据具备统一存储模型，支持后续统计与看板展示。
