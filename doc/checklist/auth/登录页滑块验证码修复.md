# 登录页滑块验证码修复清单

## 1. 目标
- [x] 登录页在 `login-config` 拉取失败时仍展示滑块验证码，不再静默关闭安全校验。
- [x] 登录按钮在验证码未完成前保持受控，不会因为配置加载失败而放开登录。

## 2. 开发任务
- [x] 调整 `PasswordLogin` 的配置失败兜底策略，失败时默认回退到滑块验证码。
- [x] 为 `PasswordLogin` 增加回归测试，覆盖配置请求失败场景。

## 3. 验证
- [x] `pnpm --dir UnionDeskWeb/apps/UnionDeskAdminWeb exec vitest run src/pages/login/components/password-login.test.tsx`
- [ ] `pnpm --dir UnionDeskWeb/apps/UnionDeskAdminWeb typecheck`（当前仓库存在既有类型错误，与本次修复无关）
