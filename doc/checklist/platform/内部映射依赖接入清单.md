# 内部映射依赖接入清单

## 业务域
- 平台管理 / 部署与运行

## 目标
- 将后端运行依赖切换到本次映射到 `127.0.0.1` 的内部 MySQL。
- 创建应用专用数据库和应用用户，避免后端直接使用 root 或通用库。
- 验证 Redis、MinIO 和远程浏览器端口的可访问性，为后续功能接入保留依据。

## 开发任务
- [x] 确认端口映射可访问：远程浏览器 `32561`、Redis `30379`、MySQL `30306`、MinIO API `30090`、MinIO Console `30091`。
- [x] 创建应用数据库 `uniondesk`。
- [x] 创建应用用户 `uniondesk_app` 并授予 `uniondesk.*` 权限。
- [x] 将后端 datasource 切换到 `127.0.0.1:30306/uniondesk`。
- [x] 将 Flyway Maven 插件连接信息同步切换到 `127.0.0.1:30306/uniondesk`。
- [x] 同步 compose 中 MySQL、Redis、MinIO 的默认密钥口径。
- [x] 重启后端并验证服务可用。

## 验证记录
- [x] Redis 使用密码认证后返回 `PONG`。
- [x] MinIO API 健康检查返回 `200 OK`。
- [x] MinIO S3 签名请求返回 `ListAllMyBucketsResult`。
- [x] MySQL `uniondesk_app` 可登录 `uniondesk`，`log_bin_trust_function_creators = 1`。
- [x] 后端 `/api/v1/health` 返回 `{"status":"UP"}`。
- [x] 后端 `/api/v1/auth/login-config` 返回 `200`，确认认证配置可读。
- [x] 后端 `/api/v1/auth/login` 已进入业务校验，当前因验证码开启返回“验证码校验失败”。
- [x] `flyway_schema_history` 成功迁移记录为 39 条。
