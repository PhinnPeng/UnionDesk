# 平台管理端返回业务端按钮权限控制清单

> 业务域：`platform`
> 目标：当用户在平台端且不具备业务域权限时，不显示“返回业务端”按钮。

## 任务拆解

1. [x] 新增业务域访问判断工具 `hasBusinessDomainAccess()`，并补单元测试。
2. [x] 更新 `platform-entry-button.tsx`，在平台端根据业务域权限决定是否渲染按钮。
3. [x] 统一按钮视觉样式，保持与 Header 其他按钮一致。
4. [x] 补充 BDD 风格测试，覆盖“无业务域权限隐藏按钮”场景。

## 验收标准

- [x] 平台端且无业务域权限时，不显示“返回业务端”按钮。
- [x] 平台端且有业务域权限时，显示“返回业务端”按钮并正常跳转。
- [x] 无平台权限时，不显示“平台管理”按钮。
- [x] `pnpm --dir UnionDeskWeb/apps/UnionDeskAdminWeb typecheck` 通过。
- [x] `pnpm --dir UnionDeskWeb/apps/UnionDeskAdminWeb exec vitest run src/utils/access/business-domain.test.ts src/api/user/utils.test.ts src/layout/layout-header/components/platform-entry-button.test.tsx --reporter dot` 通过。
