# 平台入口根路径修复

## 2026-05-13

- [x] 根路径 `/` 在登录态下优先跳转到缓存首页，不再等待 `access store` 完整加载后才决定落点
- [x] 新增 `AuthGuard` 回归测试，覆盖根路径直接进入 `/platform/home`
- [x] 已在浏览器中验证：访问 `http://localhost:3333/` 会自动进入 `http://localhost:3333/platform/home`

## 说明

- 当前修复只调整平台入口路由落点，不修改登录态、权限快照或菜单树生成逻辑。
- 继续沿用现有的首页判定规则：平台侧优先进入 `/platform/home`，后续由原有守卫补齐权限与路由状态。
