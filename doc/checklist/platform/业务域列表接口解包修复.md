# 业务域列表接口解包修复

## 目标
- 统一 `GET /api/v1/domains` 的前端解包方式，避免把分页对象直接当数组使用
- 保证用户管理、审计日志、域配置等页面可以稳定拿到业务域数组

## 已完成
- [x] `fetchBusinessDomains` 统一解包 `PageResult.list`
- [x] `fetchPlatformOverview` 复用统一的业务域入口
- [x] 回归测试覆盖分页对象和数组两种返回形态
- [x] 浏览器回验用户管理页业务域渲染正常

## 验证
- `pnpm --dir UnionDeskWeb/apps/UnionDeskAdminWeb exec vitest run src/api/platform/domain.test.ts src/api/platform/overview.test.ts`
- 浏览器打开 `/platform/user` 后，业务域列正常显示 `Default Domain / default`
