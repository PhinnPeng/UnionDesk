# 平台管理端入口修复补记

## 2026-05-08

- [x] 顶部“平台管理”入口移除 `href` 冲突，改为纯客户端路由跳转，避免点击后整页刷新回登录页。
- [x] 对应单测已从 `link` 语义改为 `button` 语义，并补充跳转与 `window.location.assign` 的行为断言。
- [x] 已通过 `typecheck` 与 `vitest` 组件级验证。
