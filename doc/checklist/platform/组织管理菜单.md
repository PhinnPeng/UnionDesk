# 组织管理菜单模块 - 开发清单

## 模块概述
在平台管理（platform）域下新增"组织管理"菜单目录，包含用户管理、组织架构、离职池、组织配置四个子菜单。

## 开发清单

### 1. 后端修改
- [x] 修复 `AdminMenuService.createMenu`：当 menu 类型且 permissionCode 为空时跳过 required button 创建

### 2. 菜单数据（通过 API 创建）
- [x] 创建目录：组织管理（id=58, scope=platform, icon=lucide:building-2）
- [x] 创建菜单：用户管理（id=59, route=/platform/user, comp=platform/user, icon=lucide:users）
- [x] 创建菜单：组织架构（id=60, route=/platform/dept, comp=platform/dept, icon=lucide:network）
- [x] 创建菜单：离职池（id=61, route=/platform/offboard-pool, comp=platform/offboard-pool, icon=lucide:user-x）
- [x] 创建菜单：组织配置（id=62, route=/platform/org-config, comp=platform/org-config, icon=lucide:settings）

### 3. 按钮权限
- [x] 用户管理 → 新增用户（id=63, perm=platform.user.create）
- [ ] 用户管理 → 编辑用户（platform.user.update）— 已存在于旧 business 域菜单(id=18)，权限码唯一不可重复创建
- [ ] 用户管理 → 停用/离职（platform.user.disable）— 已存在于旧 business 域菜单(id=19)，权限码唯一不可重复创建
- [ ] 离职池 → 恢复用户（platform.user.restore）— 已存在于旧 business 域菜单(id=21)，权限码唯一不可重复创建
- [ ] 离职池 → 删除用户（platform.user.delete）— 已存在于旧 business 域菜单(id=22)，权限码唯一不可重复创建

> **说明**：已存在的按钮权限码分布在旧 system/business 域菜单下，权限控制仍然生效。后续如需迁移可通过数据库 SQL 更新 parent_id。

### 4. 角色授权
- [x] super_admin 角色已授权新增的 5 个菜单节点（id=59,60,61,62）和 1 个按钮节点（id=63）

### 5. 前端页面
- [x] 用户管理：复用 `src/pages/platform/user/index.tsx`
- [x] 组织架构：复用 `src/pages/platform/dept/index.tsx`
- [x] 离职池：复用 `src/pages/platform/offboard-pool/index.tsx`
- [x] 组织配置：新建 `src/pages/platform/org-config/index.tsx`（占位页面）

### 6. 组件注册表
- [x] `component-registry.ts` 中添加 `platform/org-config`

### 7. 验证
- [x] 后端重启并确认端口 8080 正常
- [x] 菜单树 API 返回正确结构
- [ ] 前端登录后侧栏显示"组织管理"及子菜单
- [ ] 各子菜单页面可正常访问
