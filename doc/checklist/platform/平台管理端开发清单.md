# 平台管理端开发清单

## 已完成

- [x] 头部头像左侧平台入口按钮
- [x] 平台模式 / 业务模式切换
- [x] `/platform` 路由组与 `/platform/home`
- [x] `system/menu` 主内容背景切到布局浅灰底
- [x] `system/role` 页面复用并保持可用
- [x] `/platform/role` 已独立为平台角色页入口，内部复用 `system/role`
- [x] 平台管理切换后，菜单树与页签按业务域 / 平台域隔离
- [x] 修复平台菜单点击后被旧活动页签拉回导致的页面轮回
- [x] 页面刷新时保持当前业务端 / 平台端路由，不被登录页或默认首页覆盖
- [x] 平台页顶部面包屑隐藏平台容器前缀，仅展示当前页面
- [x] `/platform/user` 平台用户骨架列表
- [x] `/platform/dept` 平台组织骨架页
- [x] `/platform/offboard-pool` 离职池入口与骨架页
- [x] 平台用户列表接入 IAM 用户接口
- [x] 离职池接入离职池 API
- [x] 平台首页接入真实统计摘要
- [x] 将平台入口可见性从管理员角色快照切换为 `platform.*` 权限快照
- [x] P0 平台页与 `@uniondesk/shared` 契约对齐：业务域管理、工单池、站内信、附件、客户入域（邀请码/域客户）、`step-up` 二次验证弹窗；平台首页增加 P0 入口导航

验证：

- 平台入口仅在具备 `platform.*` 权限快照时可见
- 平台模式可进入 `/platform/home`
- 业务模式可从平台模式返回
- 菜单页内容背景跟随主题显示为布局浅灰底
- 平台页和业务页的菜单源、页签桶、搜索源彼此独立
- 点击平台菜单页时不再被业务页签或旧平台活动页签反向导航
- 刷新 `/system/*` 停留在业务端，刷新 `/platform/*` 停留在平台端
- 平台页顶部不再展示 `平台管理 -> 当前页`，只展示当前平台页面标题
- 平台用户、平台组织、离职池均可从平台侧栏进入并打开独立平台页签
- 平台用户页调用 `fetchPlatformUsers` 渲染 IAM 用户，并按在职 / 停用 / 角色数统计
- 离职池页调用 `fetchPlatformOffboardPoolUsers` 渲染 IAM 离职用户，并按离职人员 / 可恢复 / 本月离职统计
- 平台首页调用真实接口汇总平台域数、在职人数、离职人数与最近登录审计，并为导入任务 / 公告保留统计位
- `pnpm --dir UnionDeskWeb run check:utf8`、`pnpm --dir UnionDeskWeb --filter react-antd-admin typecheck`、`pnpm --dir UnionDeskWeb --filter react-antd-admin test -- --run` 已通过（AdminWeb 负责人本轮）

## 进行中

- [ ] 平台组织树接入组织 / 部门接口
- [ ] 导出中心
- [ ] 平台用户模块内导入
- [ ] 平台组织模块内导入

验证：

- 已新增 `platform_organization` 表、Flyway 迁移、`/api/v1/iam/organizations` 只读接口和前端平台组织 API
- `/platform/dept` 已改为真实接口渲染树和表格，前端页面测试与后端控制器测试已通过
- 真实登录链路下组织接口曾返回 `500`，已定位并修复查询字段
- 后端二次重启验收时又暴露出当前本地 Java 启动链路异常，平台组织运行态通过需等 8080 服务稳定恢复后复验
- 已新增 `/platform/import-export` 平台侧栏入口与骨架页，当前作为导出中心历史路由保留
- 当前页面应收口为导出任务视图、结果下载、异步导出和最近任务视图，不再作为统一导入页面
- 真实任务表、执行接口、失败明细下载和审计落库仍待补齐
- 平台用户页后续需要补模板下载、导入入口、预检结果与最近导入反馈
- 平台组织页后续需要补模板下载、导入入口、组织映射校验与最近导入反馈

## 待开发

- [ ] 公告中心
- [ ] 日志中心
- [ ] 屏蔽词管理
- [ ] 知识库管理

验证：

- 当前平台首页只承接入口和骨架导航，没有公告、日志、屏蔽词、知识库这些模块的成品页面

## 权限与数据

- [ ] 统一前端单数路由与数据库历史复数种子之间的命名口径
- [ ] 确认 **Flyway V1 基线及已发布增量脚本** 与 `doc/数据库设计.md` 所述当前库表最终态一致（不再维护独立 `doc/schema.sql`）

验证：

- 后端和数据库已经具备 `platform.*` 权限元数据
- 平台入口已改为读取权限快照结果，不再仅依赖管理员角色判断
