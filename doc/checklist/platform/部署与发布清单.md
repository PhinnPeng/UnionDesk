# 部署与发布清单

## 业务域
- 平台管理 / 发布与运行

## 目标
- 将单机部署、内部环境规划、CI/CD、Flyway 迁移、灰度发布、功能开关和健康检查接口统一落到 PRD，确保当前可单机运行，未来可平滑拆分服务。

## 开发任务
- [x] 在 `3.2.7 系统设置` 中补充发布控制，明确功能开关与灰度范围可配置。
- [x] 在 `4. 非功能需求` 中补充单机部署、内部环境规划、CI/CD、数据库迁移和灰度发布口径。
- [x] 在 `6. API 设计约定` 中补充 `/api/v1/health` 与 `/api/v1/readiness` 健康检查接口。
- [x] 在 `5. 数据模型` 中补充 `feature_flag` 功能开关模型。
- [x] 在 `7. MVP 验收标准` 中补充部署、环境、迁移、CI/CD、健康检查与灰度验收项。
- [x] 将 README 中基础依赖启动口径调整为按需参考，不再写成每次开发或部署都必须先启动。
- [x] Flyway 活跃目录已切换到 `UnionDesk/src/main/resources/db/migration/current/`，历史脚本已归档到 `archive/`，新基线可通过 `clean + migrate` 重放。

## 验证记录
- [x] 当前部署架构已明确为单机模块化单体。
- [x] 开发、测试、预发布和生产环境均已定义为内部服务器环境。
- [x] Flyway 已作为数据库迁移方案写入 PRD。
- [x] 功能开关和健康检查接口已写入 PRD。
- [x] README 中基础依赖说明已同步为按需参考口径。
- [x] 新的重基线迁移 `V202605200002__rebaseline_current_schema.sql` 已生成并可重放当前 schema 与稳定种子数据。
