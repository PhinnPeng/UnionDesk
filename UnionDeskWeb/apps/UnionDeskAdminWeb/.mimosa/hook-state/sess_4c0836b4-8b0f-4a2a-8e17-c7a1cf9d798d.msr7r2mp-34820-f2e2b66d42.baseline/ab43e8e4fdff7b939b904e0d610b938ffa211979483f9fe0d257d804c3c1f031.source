import { buildTicketConfigPath } from "#src/pages/platform/ticket-config/ticket-config-path";

import { Navigate } from "react-router";

export default function PlatformTicketStatusesLegacyRedirect() {
	return <Navigate to={buildTicketConfigPath({ section: "statuses" })} replace />;
}
