import { buildTicketConfigPath } from "#src/pages/platform/ticket-config/ticket-config-path";

import { Navigate } from "react-router";

export default function PlatformTicketAttributesLegacyRedirect() {
	return <Navigate to={buildTicketConfigPath({ section: "attributes" })} replace />;
}
