import { Navigate } from "react-router";

/** 旧路径 `/platform/ticket-config/attributes` 重定向到事项配置首页（默认展示事项属性） */
export default function TicketConfigLegacyRedirect() {
	return <Navigate to="/platform/ticket-config" replace />;
}
